import './Header.scss'

function Header() {

    return(
        <header className="header">
            <h1 className="header__title">Kanban :: React</h1>
        </header>
    );
}

export default Header